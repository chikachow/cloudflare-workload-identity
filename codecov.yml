ignore:
  - "test/**/*"
  - "**/*.test.ts"
  - "workers/**/*.generated.d.ts"

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: "diff, flags, files"
  require_changes: true
